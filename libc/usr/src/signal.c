#include <signal.h>

sighandler_t signal(int sig, sighandler_t handler) {
    // To be implemented
    // __errno(ENOSYS);
    return -1;
}

int raise(int signal) {
    // To be implemeneted
    return -1;
}